# Environment variable template
# This file shows all available environment variables
# Vite automatically loads .env.development (dev) or .env.production (build)

# Backend API base URL
# - Development: Use empty string to leverage Vite proxy (avoids CORS)
# - Production: Use your actual backend URL (e.g., https://api.udonggeum.com)
VITE_API_BASE_URL=

# Development proxy target (only used in vite.config.ts during dev)
# This is the actual backend server that Vite proxy will forward to
VITE_PROXY_TARGET=http://192.168.71.112:8080

# Mock API (MSW - Mock Service Worker)
# - Set to 'true' to enable mock API (useful when backend is down)
# - Set to 'false' or omit to use real backend
VITE_MOCK_API=false
